'use client';import{useEffect,useState}from'react';import Link from'next/link';import{api,logout}from'../../lib/api';export default function Admin(){const[s,setS]=useState(),[u,setU]=useState([]),[e,setE]=useState('');useEffect(()=>{Promise.all([api('/auth/me/'),api('/admin/stats/'),api('/admin/users/')]).then(([m,a,b])=>{if(m.role!=='admin')throw Error('Administrator access required');setS(a);setU(b)}).catch(x=>setE(x.message))},[]);if(e)return <main className="container page"><p className="error">{e}</p><Link href="/login">Login</Link></main>;if(!s)return <main className="container page">Loading admin...</main>;return <><div className="adminbar">CyberGuard Administration</div><nav className="nav"><Link className="logo" href="/">Cyber<span>Guard</span></Link><div className="links"><Link href="/dashboard">User Site</Link><button className="button red" onClick={()=>{logout();location.href='/login'}}>Logout</button></div></nav><main className="container page"><p className="eyebrow">ADMINISTRATOR</p><h1>Platform Overview</h1><div className="cards"><div className="card"><div className="stat">{s.users}</div>Users</div><div className="card"><div className="stat">{s.scenarios}</div>Scenarios</div><div className="card"><div className="stat">{s.attempts}</div>Attempts</div></div><div className="card"><h2>Registered Users</h2><table><thead><tr><th>Username</th><th>Email</th><th>Role</th><th>Points</th></tr></thead><tbody>{u.map(x=><tr key={x.id}><td>{x.username}</td><td>{x.email}</td><td>{x.role}</td><td>{x.points}</td></tr>)}</tbody></table></div></main></>}
